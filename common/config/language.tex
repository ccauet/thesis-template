%!TEX root = ../../main.tex

% correct, language-sensitive punctuation
\usepackage[style=british]{csquotes}
% From the csquotes guide: 
% "Note that polyglossia support is currently in a preliminary state because
% polyglossia is lacking a proper interface for other packages. In practice,
% this means that csquotes can detect the language (e.g., english) but not the
% language variant (e.g., british)."

% language-sensitive settings
\usepackage{polyglossia}
\setdefaultlanguage[variant=british]{english}
\setotherlanguage{german}