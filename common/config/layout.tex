%!TEX root = ../../main.tex

% headings
\usepackage{scrpage2}
\pagestyle{scrheadings}

% captions
% \usepackage[format=plain,labelsep=newline,singlelinecheck=true,font=it,labelfont=normalfont]{caption}
\usepackage{caption}
  \captionsetup{labelfont=bf}
  \captionsetup{font=small}
  \captionsetup{format=plain}
  \captionsetup{labelsep=endash}

% appendix
\usepackage[toc]{appendix}
% to change name of appendix in toc, use "\renewcommand{\appendixtocname}{New appendix name}" in your private config

% prevent orphans and widows
% \widowpenalty10000
% \clubpenalty10000